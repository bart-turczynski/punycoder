% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{unicode_versions}
\alias{unicode_versions}
\title{Unicode table sets available in this build}
\usage{
unicode_versions()
}
\value{
A character vector of Unicode version strings, e.g. \code{"16.0.0"}.
}
\description{
punycoder vendors its Unicode data (combining classes, decompositions, UTS
#46 mapping and status, \code{Bidi_Class}, \code{Joining_Type}) as generated tables
compiled into the package, and a build can carry more than one version at
once. This reports the versions it carries, in registration order.
}
\details{
Which one \code{\link[=host_normalize]{host_normalize()}} uses by default is the \emph{pinned} version, and it
is reported by \code{normalization_profile_info()$unicode_version} rather than
marked here — that column is the single source of truth downstream packages
key on.
}
\examples{
unicode_versions()
normalization_profile_info()$unicode_version # the pinned default
}
\seealso{
\code{\link[=host_normalize]{host_normalize()}} for selecting one,
\code{\link[=normalization_profile_info]{normalization_profile_info()}} for the pinned default and the rest of the
profile identity.
}
